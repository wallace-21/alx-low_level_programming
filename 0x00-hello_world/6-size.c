#include <stdio.h>

/**
 * main - prints the size of various types on
 * the computer it is compiled and run on..
 * Return: Always 0.
 */

int main(void)

{
	printf("size or a char: %zu byte(s)\n", sizeof(char));

	printf("size of an int: %zu byte(s)\n", sizeof(int));
